{
  "type": "object",
  "required": [
    "name",
    "components"
  ],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "metadata": {
      "type": "object"
    },
    "components": {
      "type": "array",
      "items": {
        "required": [
          "name",
          "type",
          "version"
        ],
        "properties": {
          "name": {
            "type": "string",
            "message": {
              "maxLength": "component name must be <= 20 characters long"
            },
            "pattern": "^[a-z][a-z0-9-]*$",
            "maxLength": 20
          },
          "type": {
            "type": "string"
          },
          "version": {
            "type": "string"
          }
        }
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "description": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
